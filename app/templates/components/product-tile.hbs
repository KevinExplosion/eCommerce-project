<h4>{{product.title}}</h4>
<h5>{{product.cost}}</h5>
  <div class="row">
    <div class="col-md-10">
      <div class="col-md-4">
        <p><img src="{{product.image}}" alt="{{product.title}}"></p>
      </div>

      <div class="col-md-8">
        {{product.description}}
      </div>

    </div>
  </div>
<hr>

<button {{action "addToCart" product}}>Add to Cart</button>
